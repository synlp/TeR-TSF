# GPU显存清理脚本使用说明

## 概述

`gpu_memory_cleanup.sh` 是一个用于清理僵尸进程占用GPU显存的自动化脚本。当深度学习训练进程被异常终止后，有时会出现进程已被kill但GPU显存仍被占用的情况，这个脚本可以自动识别并清理相关的残留进程。

## 问题背景

在深度学习训练中，特别是使用PyTorch等框架的多进程训练时，可能会遇到以下情况：
- 主进程被kill后，GPU显存仍被占用
- `nvidia-smi` 显示进程为 `[Not Found]` 或 `No Such Process`
- 相关的worker进程（如 `pt_data_worker`）仍持有CUDA文件描述符
- GPU显存无法正常释放

## 脚本功能

1. **进程状态检查** - 验证目标PID是否仍在运行
2. **GPU显存检测** - 确认该PID是否占用GPU显存
3. **文件描述符分析** - 查找相关的CUDA UVM文件描述符
4. **相关进程识别** - 找出持有GPU设备文件的worker进程
5. **安全清理** - 在用户确认后清理相关进程
6. **结果验证** - 确认GPU显存是否成功释放

## 使用方法

### 基本用法
```bash
./gpu_memory_cleanup.sh <PID>
```

### 示例
```bash
# 清理PID为512626的进程占用的GPU显存
./gpu_memory_cleanup.sh 512626
```

## 脚本执行流程

1. **参数验证** - 检查PID格式是否正确
2. **进程检查** - 确认目标进程状态
3. **显存检测** - 使用nvidia-smi检查GPU显存占用
4. **相关进程查找** - 通过lsof和fuser找出相关进程
5. **用户确认** - 显示将要清理的进程信息，等待用户确认
6. **进程清理** - 使用kill -9强制终止相关进程
7. **结果验证** - 确认GPU显存是否成功释放
8. **状态报告** - 显示当前GPU使用状态

## 输出示例

```
[INFO] 开始清理PID 512626 占用的GPU显存...
[INFO] 检查进程状态...
[INFO] 进程 512626 已不存在，检查GPU显存占用...
[INFO] 检查GPU显存占用情况...
[WARNING] 发现显存占用: 512626,[Not Found],56816
[INFO] 查找相关的CUDA文件描述符...
[INFO] 找到相关的进程PID: 554055 554057 554058 554059 554060 554061 554062 554063 554064 554065 554066 554067
[INFO] 检查GPU设备文件使用情况...
[INFO] 准备清理以下进程: 554055 554057 554058 554059 554060 554061 554062 554063 554064 554065 554066 554067
[INFO] 相关进程信息:
554055 1254364 pt_data_worker
554057 1254364 pt_data_worker
...
确认清理这些进程？(y/N): y
[INFO] 正在清理相关进程...
[INFO] 终止进程 554055...
[INFO] 终止进程 554057...
...
[INFO] 等待GPU显存释放...
[INFO] 验证清理结果...
[SUCCESS] GPU显存清理成功！PID 512626 不再占用GPU显存
[INFO] 当前GPU状态:
index, name, memory.used [MiB], memory.total [MiB]
0, NVIDIA H800, 60627, 81559
1, NVIDIA H800, 4, 81559
...
[INFO] 清理操作完成
```

## 安全特性

- **用户确认** - 在清理进程前会显示详细信息并要求用户确认
- **错误处理** - 使用 `set -e` 确保脚本在遇到错误时停止
- **进程验证** - 在清理前验证进程是否存在
- **结果验证** - 清理后验证是否成功释放显存

## 注意事项

1. **权限要求** - 脚本需要能够查看和终止用户自己的进程
2. **谨慎使用** - 只清理与目标PID相关的进程，但仍建议在清理前确认
3. **备用方案** - 如果脚本无法解决问题，可能需要重启系统或联系管理员
4. **多GPU环境** - 脚本会检查所有GPU设备，自动识别相关进程

## 故障排除

### 问题1：脚本提示"未发现PID占用GPU显存"
- 可能该PID确实没有占用GPU显存
- 检查PID是否正确

### 问题2：清理后仍有显存占用
- 可能存在其他类型的资源泄漏
- 建议重启相关服务或系统

### 问题3：权限不足
- 确保有足够权限查看和终止进程
- 不需要sudo权限，但需要能操作自己的进程

## 技术原理

脚本基于以下技术原理：
1. **CUDA UVM** - 通过lsof查找CUDA统一虚拟内存文件描述符
2. **设备文件** - 通过fuser查找使用GPU设备文件的进程
3. **进程关系** - 识别与目标PID相关的worker进程
4. **强制清理** - 使用SIGKILL信号强制终止残留进程

## 维护

如需修改或扩展脚本功能，请注意：
- 保持错误处理机制
- 维护用户确认流程
- 测试不同的GPU和CUDA版本兼容性 